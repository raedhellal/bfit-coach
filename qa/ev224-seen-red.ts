// EV-224 AC2 — THROWAWAY. One deliberate type error so the CI run is seen red at
// `npm run typecheck`. This branch is never merged; delete it after the run URL is recorded.
export const seenRed: number = "EV-224 AC2";
